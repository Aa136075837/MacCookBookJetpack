<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.mac.macjetpackdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/cook_logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/cook_logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".demo.PagingDemoActivity"></activity>
        <activity android:name=".ui.type.TypeActivity"></activity>
        <activity android:name=".ui.detail.DetailActivity"></activity>
        <activity android:name=".ui.search.SearchResultActivity"></activity>
        <activity android:name=".ui.MainActivity">
        </activity>
    </application>

</manifest>